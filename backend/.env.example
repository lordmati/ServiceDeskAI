# MongoDB Configuration
MONGO_URI=mongodb://mongo:27017/servicedeskai

# JWT Secret Key (Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_super_secret_jwt_key_here_replace_this_with_generated_key

# Email Configuration (Gmail SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password_here
EMAIL_FROM="ServiceDesk AI <noreply@servicedesk.com>"

# Frontend URL (for email links)
APP_URL=http://localhost:3000